# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
# 0.0.20+ fixes CVE-2024-24762 (Content-Type header ReDoS), CVE-2024-53981 (DoS)
python-multipart==0.0.20
aiofiles==23.2.1

# Data validation
pydantic==2.5.3
pydantic-settings==2.1.0

# Distributed computing
# 2.50.0: latest line with both amd64 AND arm64/v8 Docker images (required
# for Oracle Cloud Always Free / Ampere A1 ARM deployment)
ray[default]==2.50.0

# Caching / job state
redis==5.0.1

# Data processing
pandas==2.2.0

# Cloud storage (optional — S3 backend)
boto3==1.34.34

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0
